<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= title || 'Superhéroes App' %></title>
  <link rel="stylesheet" href="/styles.css">
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

</head>
<body class="min-h-screen bg-fixed bg-cover bg-center bg-no-repeat" style="background-image: url('/images/avengers.jpg')">
  <div class="relative min-h-screen flex flex-col">

    <!-- Overlay oscuro -->
    <div class="absolute inset-0 bg-black opacity-50 z-0"></div>

    <!-- Contenido principal con z-index superior -->
    <div class="relative z-10 flex flex-col min-h-screen">
      
      <!-- Navbar -->
      <%- include('partials/navbar') %>

      <!-- Contenido dinámico -->
      <main class=" flex-grow container mx-auto px-2 py-32 text-white">
        <%- body %>
      </main>

      <!-- Footer -->
      <%- include('partials/footer') %>

    </div>
  </div>
</body>

</html>